name: yibi
description: 一笔（OneEntry）— 本地优先的多人记账应用。
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  path: 1.9.1
  sqflite: 2.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/branding/oneentry_icon.png
